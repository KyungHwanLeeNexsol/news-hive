## Task Decomposition
SPEC: SPEC-AI-076

| Task ID | Description | Requirement | Dependencies | Planned Files | Status |
|---------|-------------|-------------|--------------|---------------|--------|
| T-001 | 재현 우선(Rule 4) 실패 characterization — Pool A/B/C 소스 mock(A raw=232, B=0, C=52 서로소, existing=∅, cap=150). 현행 concat-then-slice가 `entry_pool=="pool_c"` 대표를 0으로 만드는 것을 포착하고 목표 단언(pool_c 대표 >= 30, pool_a <= 120, len==150)에서 실패함을 확인(수정 전 RED). | REQ-006 (001/003 목표) | - | backend/tests/test_spec_ai_065.py | pending |
| T-002 | (거동 무변경 선행) SurgeDetectionConfig에 `pool_b_min_slots: int = 20` / `pool_c_min_slots: int = 30` Pydantic 필드 + surge_detection.yaml 대응 키 추가. 배분 로직 미변경이라 T-001 RED 여전히 실패 확인(미참조 필드는 버그 미해결 → Rule 4 보존). Pydantic v2가 미선언 속성 대입을 금지하므로 core GREEN 확인 이전 필드 선언 필수(순서 근거는 아래 기술 결정 참조). | REQ-007 | T-001 | backend/app/surge_config/surge_settings.py, backend/app/surge_config/surge_detection.yaml | pending |
| T-003 | (핵심 IMPROVE) `build_scan_universe` 최종 병합(:4288-4303) quota화 — `reserved_b=min(len(pool_b),F_b)`/`reserved_c=min(len(pool_c),F_c)`, `sum_reserved>cap`면 비율 축소+경고(clamp), 예약분 선확보 → 잔여를 A>B(나머지)>C(나머지)>existing 우선순위로 채움 → dedup(순서 보존) → `[:cap]` 안전 절단. `floors==0`이면 레거시 concat-then-slice와 정확 동일. T-001 GREEN 확인. | REQ-001/002/003/004 | T-002 | backend/app/services/surge_detector.py | pending |
| T-004 | (관측성, 스키마 0) `final_universe` 확정 후 `Counter(entry_pool_map[c] for c in final_universe)`로 `pool_a_scanned`/`pool_b_scanned`/`pool_c_scanned` 산출해 반환 `pool_counts`에 신규 키로 추가(기존 raw `pool_a/b/c` 키 보존) + 최종 로그에 raw 대비 scanned 병기. 호출부 `persist_pool_counts`(:1953-1962, 명시적 `.get("pool_a")` 추출)는 무변경 → SurgeUniversePoolHistory raw 의미 불변. | REQ-005 | T-003 | backend/app/services/surge_detector.py | pending |
| T-005 | 잔여 AC characterization/property — AC-002(비굶주림 파라미터화 (200,10,40)/(160,0,60)/(300,25,25)), AC-003(비용 상한 & `max_scan_universe`/`_min_ratio` 리터럴 불변), AC-004(절단 압력 없음 집합 동등성 — existing 처리 정본화, 아래 기술 결정 참조), AC-005(floors=0 레거시 정확 동등), AC-006(scanned 관측성), AC-007(raw persist 의미 불변), AC-008(clamp+경고). | REQ-001~007 검증 | T-004 | backend/tests/test_spec_ai_065.py | pending |
| T-006 | (문서성, 거동 무변경) `:4183`(SPEC-AI-074)/`:4217`(SPEC-AI-067) 불변 주석을 "배분 메커니즘=quota(SPEC-AI-076 소유), `max_scan_universe`/`_min_ratio`는 여전히 불변"으로 갱신 + `build_scan_universe`에 `@MX:ANCHOR`(fan_in=3 배분 계약)·최종 배분 지점 `@MX:NOTE`(+`@MX:SPEC: SPEC-AI-076`) + surge_universe_pool_history.py `pool_a/b/c_count` = pre-truncation 의미 주석 명확화(컬럼/스키마 불변). | 횡단(문서성/REQ-005 의미) | T-003 | backend/app/services/surge_detector.py, backend/app/models/surge_universe_pool_history.py | pending |
| T-007 | (회귀 + 품질 게이트) test_spec_ai_065 / test_spec_ai_074 / test_surge_universe_members / test_surge_universe_pool_bugfix 무회귀 + 전체 백엔드 스위트(`-n 4` xdist 포함) 회귀 0 + `ruff check .` 무경고 + `mypy app/` 무오류 + 신규/변경 로직 커버리지 85%+. Exclusions diff 0 확인(탐지기/앙상블/신호발신/후보소싱/매수/스키마/상수 리터럴). | REQ-006 + Exclusions(AC-010) | T-001~T-006 | (검증 전용 — 코드 변경 없음) | pending |

Dependency graph: T-001 → T-002 → T-003 → T-004 → T-005 → T-007, with T-006 (문서성) parallel from T-003. AC 매핑: T-001=AC-001(RED), T-003=AC-001(GREEN)/002/003/005/008, T-004=AC-006/007, T-005=AC-002~008, T-007=AC-009/010.

Confirmed technical decisions (2026-07-09, manager-strategy verified against live code surge_detector.py:4108-4315):

- **plan.md AS-IS 검증 통과**: `:4288-4303`가 실제로 `pool_a+pool_b+pool_c+[existing filter]` concat 후 `universe_dedup[:max_universe]` 슬라이스임을 코드로 확인. `pool_counts`(:4282-4286)는 raw pre-truncation 길이(`pool_a/b/c` 3키, `existing` 키 없음). `_min_ratio=2.0`(:4207), `max_universe=config.max_scan_universe`(:4142, 150). 불변 주석 2건(:4183 AI-074, :4217 AI-067) 실재 확인. plan.md TO-BE quota 알고리즘은 현행 코드와 정합.
- **R2 위험 실측 완화**: 관측성 신규 키(`*_scanned`) 회귀 위험(raw 의미 오염)이 낮음 — persist 호출부(:1953-1962)가 `_pool_counts.get("pool_a"/"pool_b"/"pool_c")`로 **명시적 키만** 추출하므로 반환 dict에 scanned 키를 추가해도 SurgeUniversePoolHistory 쓰기에 새지 않는다. AC-076-007은 이 구조로 자연 충족(T-004에서 인자 고정 테스트로 이중 보증).
- **T-002를 core(T-003) 앞에 배치한 근거(부모 지시 "core→config"에서의 의도적 편차)**: SurgeDetectionConfig는 Pydantic v2 BaseModel(surge_settings.py:418)이라 미선언 속성 대입(`config.pool_c_min_slots=30`)이 금지된다. 따라서 core 로직 GREEN 확인 시 테스트 config가 floor를 담으려면 필드 선언이 선행돼야 한다. 대안: core에서 `getattr(config,"pool_c_min_slots",30)` 브리지 후 config 태스크에서 정식화(부모 순서 그대로 유지 가능). **권장**: 거동 무변경 필드 선언을 RED 뒤·core 앞에 두는 것(미참조 필드는 버그를 고치지 않아 RED 유지 → Rule 4 보존). 어느 방식이든 "실패 테스트 → (확인) → 실제 수정" 불변은 지켜진다.
- **[검증 플래그, DDD ANALYZE에서 반드시 해소] AC-076-004 vs 현행 existing 처리 불일치**: 현행 코드는 :4278-4280에서 모든 `existing_codes`를 `entry_pool_map`에 "existing"으로 등록한 뒤 :4293에서 `[c for c in existing_codes if c not in entry_pool_map]`로 병합하는데, 앞 루프가 전부 등록했으므로 이 필터는 **항상 빈 리스트**다 → 순수 existing 종목은 사실상 `final_universe`에서 제외되는 잠재(pre-existing) 거동. AC-076-004는 existing 5건(서로소)이 `final_universe`에 **전부 포함**(합 35)된다고 단언하나, 현행은 30건(existing 제외)이다. REQ-004의 "기존 거동과 동일" 요구와 AC-076-004가 상충한다. **해소 방안(DDD PRESERVE에서 현행 existing 거동을 characterization으로 먼저 고정한 뒤 택1)**: (1) 잠재 제외 거동 보존 → AC-076-004를 "existing 제외, 30건"으로 정정(엄격 "배분 메커니즘만" 범위 유지, 권장), 또는 (2) plan.md 잔여 채움 순서에 명시된 "existing"을 실제 포함시켜 잠재 버그 교정 → AC-076-004 원문 충족하되 순수 배분 교체를 넘는 의도적 거동 변경으로 문서화. 이 결정은 human/DDD 승인 필요.
- **비례성(proportionality) 판정**: 변경은 단일 함수 배분 블록(~15줄 → quota ~20-30줄) + 설정 2필드 + 테스트 + 주석에 집중. 신규 클래스/모듈/추상화 없음, 파일 5개(부모 명시)와 정확히 일치, 오버엔지니어링/스코프 크립 없음. 기본값 20/30(합 50 << 150)로 clamp 여유 충분. 관측성은 `Counter` 인라인(스키마 0). 유일한 스코프 확장 리스크는 위 existing 처리 결정(2번 택시).
- fan_in=3 확인: `surge_detector.py:1933`(gather_surge_candidates), `scheduler.py:1226`, `:1243`. scanned 로깅/노출의 자연 지점은 :1933 경로(직후 persist_pool_counts/persist_universe_members).
